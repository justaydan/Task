@model TblTransorders
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@if (Model == null)
{
    <h1>    hellooo</h1>

}`


    <div class="form-group row">
        <label class="col-sm-2 col-form-label">Yuk gonderen</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" asp-for="@Model.OrdFclient">
        </div>
    </div>
    <div class="form-inline row">
        <label class="col-sm-2 col-form-label">Yukleme stansiyasi</label>
        <div class="col-sm-10">
            @foreach (var item in ViewBag.v)
            {
                
                    <span class="col-sm-2 col-form-label">Kod</span>
                    <input type="text" id="yksid" class="form-control"  value="@item.PntCode">
                    <span class="col-sm-2 col-form-label">Ad</span>
                    <input type="text" id="yks" class="form-control"  value="@item.PntName">
                    <input type="hidden" asp-for="OrdFpoint" value="@item.PntRecno" />
                    break;
                
            }
        </div>
    </div>
    <br />
    <div class="form-group row">
        <label class="col-sm-2 col-form-label">Yuk Alan</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" asp-for="OrdFclient">
        </div>
    </div>
    <div class="form-inline row">
        <label class="col-sm-2 col-form-label">Teyinat stansiyasi</label>
        <div class="col-sm-10">
            @foreach (var item in ViewBag.g)
            {
               
                    <span class="col-sm-2 col-form-label">Kod</span>
                    <input type="text" id="tysid" class="form-control" value="@item.PntCode">
                    <span class="col-sm-2 col-form-label">Ad</span>
                    <input type="text" id="tys" class="form-control" value="@item.PntName">
                    <input type="hidden" asp-for="OrdTpoint" value="@item.PntRecno" />

                    break;
               
            }
        </div>
    </div>

    <br />
    <div class="form-inline row">
        <label class="col-sm-2 col-form-label">Serhed noqteleri</label>
        <div class="col-sm-10">
            @foreach (var item in ViewBag.g1)
            {
                
                    <span class="col-sm-2 col-form-label">Giris</span>

                    <select class="form-control  col-sm-9 mx-2 " asp-for="OrdBegpoint" asp-items="@(new SelectList(ViewBag.vall,"PntRecno","PntName",item.PntName))">
                       
                       
                    </select>

                break;




            }
            @foreach (var item in ViewBag.g2)
            {
                
                    <span class="col-sm-2 col-form-label">Cixish</span>

                    <select class="form-control  col-sm-9 mx-2 " asp-for="OrdEndpoint" asp-items="@(new SelectList(ViewBag.vall,"PntRecno","PntName",item.PntName))">
                       
                    </select>
                    break;


                  
            }

            <span class="col-sm-2 col-form-label">Mesafe</span>
            <input type="text" class="form-control col-sm-1" asp-for="OrdDistance">
        </div>
    </div>
    <br />

    <div class="form-inline row">
        <label class="col-sm-2 col-form-label">Istenilen Qiymet st1</label>
        <div class="col-sm-10">
            @foreach (var item in ViewBag.g3)
            {
               
                    <span class="col-sm-2 col-form-label">Kod</span>
                    <input type="text" id="istid1" class="form-control" value="@item.PntCode">
                    <span class="col-sm-2 col-form-label">Ad</span>
                    <input type="text" id="ist1" class="form-control" value="@item.PntName">
                    <input type="hidden" asp-for="OrdStartid" value="@item.PntRecno" />
                    break;
                
            }
        </div>
    </div>

    <br />
    <div class="form-inline row">
        <label class="col-sm-2 col-form-label">Istenilen Qiymet st2</label>
        <div class="col-sm-10">
            @foreach (var item in ViewBag.g4)
            {
               
                    <span class="col-sm-2 col-form-label">Kod</span>
                    <input type="text" id="istid2" class="form-control" value="@item.PntCode">
                    <span class="col-sm-2 col-form-label">Ad</span>
                    <input type="text" id="ist2" class="form-control" value="@item.PntName">
                <input type="hidden" asp-for="OrdEndid" value="@item.PntRecno" />
                    break;
                
            }
        </div>
    </div>
    <br />
    <div class="form-inline row">
        <label class="col-sm-2 col-form-label">YUK(QNQ)</label>
        <div class="col-sm-10">

            <span class="col-sm-2 col-form-label">Kod</span>
            <input type="text" class="form-control" asp-for="OrdLoadstcardcode1">
            <span class="col-sm-2 col-form-label">Ad</span>
            <input type="text" class="form-control" value="">

        </div>
    </div>
    <br />

    <div class="form-inline row">
        <label class="col-sm-2 col-form-label">YUK(ETSNQ)</label>
        <div class="col-sm-10">

            <span class="col-sm-2 col-form-label">Kod</span>
            <input type="text" class="form-control" asp-for="OrdLoadstcardcode1">
            <span class="col-sm-2 col-form-label">Ad</span>
            <input type="text" class="form-control" value="">

        </div>
    </div>
    <br />



    <div class="form-group shadow-textarea">
        <label for="exampleFormControlTextarea6">Qeyd</label>
        <textarea class="form-control z-depth-1" id="exampleFormControlTextarea6" rows="3" placeholder="Write something here..."></textarea>
    </div>




<script>
    $(document).ready(function () {
        $('#ist1').autocomplete({
            source: '@Url.Action("GetIstn1", "Home")',
            select: function (event, ui) {
                //set tagids to save
                $("#istid1").val(ui.item.id);
                $("#OrdStartid").val(ui.item.recno);


                //Tags for display
                this.value = ui.item.value;
                return false;
            }
        });
         $('#istid1').autocomplete({
            source: '@Url.Action("GetIstnid1", "Home")',
            minLength: 1,
            select: function (event, ui) {
                //set tagids to save
                $("#ist1").val(ui.item.id);
                $("#OrdStartid").val(ui.item.recno);

                //Tags for display
                this.value = ui.item.value;
                return false;
            }
         });


         $('#ist2').autocomplete({
            source: '@Url.Action("GetIstn1", "Home")',
            select: function (event, ui) {
                //set tagids to save
                $("#istid2").val(ui.item.id);
                $("#OrdEndid").val(ui.item.recno);

                //Tags for display
                this.value = ui.item.value;
                return false;
            }
        });
         $('#istid2').autocomplete({
            source: '@Url.Action("GetIstnid1", "Home")',
            minLength: 1,
            select: function (event, ui) {
                //set tagids to save
                $("#ist2").val(ui.item.id);
                $("#OrdEndid").val(ui.item.recno);

                //Tags for display
                this.value = ui.item.value;
                return false;
            }
         });

         $('#tys').autocomplete({
            source: '@Url.Action("GetIstn1", "Home")',
            select: function (event, ui) {
                //set tagids to save
                $("#tysid").val(ui.item.id);
                $("#OrdTpoint").val(ui.item.recno);


                //Tags for display
                this.value = ui.item.value;
                return false;
            }
        });
         $('#tysid').autocomplete({
            source: '@Url.Action("GetIstnid1", "Home")',
            minLength: 1,
            select: function (event, ui) {
                //set tagids to save
                $("#tys").val(ui.item.id);
                $("#OrdTpoint").val(ui.item.recno);

                //Tags for display
                this.value = ui.item.value;
                return false;
            }
        });


         $('#yks').autocomplete({
            source: '@Url.Action("GetIstn1", "Home")',
            select: function (event, ui) {
                //set tagids to save
                $("#yksid").val(ui.item.id);
                $("#OrdFpoint").val(ui.item.recno);

                //Tags for display
                this.value = ui.item.value;
                return false;
            }
        });
         $('#yksid').autocomplete({
            source: '@Url.Action("GetIstnid1", "Home")',
            minLength: 1,
            select: function (event, ui) {
                //set tagids to save
                $("#yks").val(ui.item.id);
                $("#OrdFpoint").val(ui.item.recno);

                //Tags for display
                this.value = ui.item.value;
                return false;
            }
        });
    });

</script>